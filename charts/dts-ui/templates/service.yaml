apiVersion: v1
kind: Service
metadata:
  name: dts-ui
  namespace: {{.Release.Namespace}}
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: dts-ui
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30001